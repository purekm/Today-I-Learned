MetricServer deploy 하는 법
![alt text](image.png)

metricServer는 외부 tool이라서 github를 통해 다운로드해옴

---

MetricServer를 통해 지표 확인하는 법

metricServer을 다운로드하고, kubectl top node 처럼 top 명령어를 사용
metricserver가 없으면 top 명령어는 에러처리

---
