#include <iostream>
#include <vector>
#include <algorithm>
#include <queue>

using namespace std;

int study[100001][2];

int main(){
    int n,k,cnt,last;
    cin >> n ;
    cnt = 0;
    last = 0;
    for(int i=0;i<n;i++){
        cin >> study[i][0] >> study[i][1];
        
    }
    sort(finish,finish+n);
    while(last<finish[n-1]){
        if(start[i]>=last)
    }
    
    return 0;
}