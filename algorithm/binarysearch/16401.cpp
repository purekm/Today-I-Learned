#include <iostream>
#include <vector>
#include <algorithm>
#include <queue>
#include <string.h>

using namespace std;
int nephew[1000001];
int snack[1000001];


int main(){
    cin.tie(nullptr);
    ios::sync_with_stdio(false);
    int m,n;
    cin >> m >> n;
    for(int i=0;i<n;i++) cin >> snack[i];
    return 0;
}