Q101
프라이빗 서브넷과 퍼블릭 서브넷이 있으면, 퍼블릭 서브넷에 NAT를 설정해야 뒤에 있는 프라이빗 서브넷에 IP를 줄 수 있음

Q102 
온프레미스 SFTP는 DataSync 보다 Transfer Familiy를 사용해야 함
AWS DataAgent는 온프레미스 스토리지 에서 AWS로 데이터를 복사할 때 사용함

Q104
GuardDuty는 계정 보호 서비스
CloudFront로도 DDos에 대처 가능

Q105
실행역할은 Lambda가 실행될 때 리소스를 사용하려고 임시로 만든 IAM 역할
리소스 기반 정책은 외부에서 Lambda를 호출하기위함

Q116
AWS WAF는 동적 컨텐츠 방어용

Q117 
CloudWatch Logs를 구독하면 실시간에 가깝게 OpenSearchService로 스트리밍 가능

Q119 
Firewall Manager은 중앙에서 Firewall 관리

Q121
Amazon RDS DB 인스턴스의 암호화는 인스턴스를 생성할 때에만 가능하며 DB 인스턴스가
생성된 후에는 불가능 
암호화되지 않은 스냅샷의 사본을 암호화할 수 있기 때문에 암호화되지 않은 DB 인스턴스에 실질적으로 암호화를 추가할 수 있음  

EBS는 여러 EC2 인스턴스에 동시 접근 불가

Q130
CPU 사용률에 따라서 성능 차이가 날때 성능을 추적해보려는 경우 대상 추적 정책을 사용함

S3 + CloudFront 를 사용하는 상황에서 S3 에 직접 액세스하는 것을 막으려면 OAC 또는
OAI 를 사용하면 됨.

Q133
RDS Custom은 데이터베이스 관리 작업 및 운영을 자동화하면서, 환경 및 운영체제 접근에 custom 가능

Q136
복제는 항상 스냅샷(인스턴스)만 가능

Q138
AWS MQ에 Rabbit MQ가 포함되어 있음