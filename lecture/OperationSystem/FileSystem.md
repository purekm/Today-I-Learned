# Windows vs Linux 파일 시스템 비교

---

## 파일 시스템 기본 개념

파일 시스템은  
저장장치(HDD, SSD 등)에 데이터를 체계적으로 저장하고 접근하는 방법

운영체제는 이를 통해 파일의 **저장 위치, 크기, 권한, 구조**를 관리함.

---

## Windows의 파일 시스템

### 주요 파일 시스템

| 파일 시스템 | 설명 | 특징 |
|--------------|------|------|
| **FAT / FAT32** | 초기 Windows에서 사용 | 구조 단순, 호환성 높지만 4GB 파일 제한 |
| **NTFS** | Windows NT 이후 기본 | 저널링, 보안, 복구, 대용량 지원 |


---

### NTFS 구조 핵심

- 모든 파일과 디렉토리를 **MFT(Master File Table)** 로 관리  
- **메타데이터 및 실제 데이터**를 하나의 테이블로 통합 관리 (ext4의 Inode 테이블 느낌)  
- 장애 복구를 위한 **저널링(Journaling)** 지원 
- - 저널링은 디스크에 실제 변경하기전에 계획 및 상황을 로그에 남기는, 파일 시스템의 트랜잭션 로그
- 파일/폴더 단위의 **ACL(Access Control List)** 보안 구조   

---

##  Linux의 파일 시스템

### 주요 파일 시스템

| 파일 시스템 | 설명 | 특징 |
|--------------|------|------|
| **ext2** | 초기 리눅스 기본 FS | 단순 구조, 저널링 없음 |
| **ext3** | ext2 + 저널링 추가 | 안정성 강화 |
| **ext4** | 현재 표준 | 대용량, 빠른 복구, 지연 할당 |


---

### ext4 구조 핵심

- **inode 구조**: 파일 메타데이터(크기, 위치, 권한) 저장  
- **block group 구조**: 디스크를 구역 단위로 관리해 탐색 속도 향상  
- **Superblock**: 파일시스템 전체 정보를 저장  
- **Directory Entry**: 파일명 ↔ inode 번호 연결  
- **Journal 영역**: 변경사항 로그 저장 → 빠른 복구 가능  


---

## ⚖️ NTFS vs ext4 비교

| 항목 | **NTFS (Windows)** | **ext4 (Linux)** |
|------|--------------------|------------------|
| 구조 | MFT 기반 (테이블) | inode 기반 (블록 단위) |
| 저널링 | 지원 (데이터+메타데이터) | 지원 (메타데이터 중심) |
| 권한 관리 | ACL 기반 세밀 제어 | POSIX 권한 + ACL 확장 |
| 성능 | 안정성↑, 부하 시 다소 무거움 | 경량, 서버용 최적화 |
| 복구 | 자동 로그 복구 | fsck, 저널링 복구 |
| 특징 | 암호화, 압축, 트랜잭션 | 지연 할당, 스냅샷(Btrfs) |
| 호환성 | Windows 전용 | Linux 전용 (ext4 기본) |

---

## 💬 한 줄 요약

> Windows는 **NTFS** 중심으로 보안·복구 기능이 강점이고,  
> Linux는 **ext4(inode 기반)** 로 경량·고성능 서버 환경에 최적화되어 있다.  
>  
> 즉, **NTFS = 안정성과 복구 중심**,  
> **ext4 = 효율성과 속도 중심**의 파일 시스템이다.

---

## 🧩 참고 키워드

- MFT (Master File Table)  
- inode / Superblock  
- Journaling (로그 기반 복구)  
- ACL (Access Control List)  
- Btrfs / ZFS / XFS  
- fsck (파일시스템 점검 도구)

---
